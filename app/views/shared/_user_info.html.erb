<%= link_to gravatar_for(current_user, size: Settings.users.gravatar.display_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("pages.users.view_link"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t("pages.micropost.text") %></span>
